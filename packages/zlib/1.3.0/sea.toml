[package]
name = "zlib"
version = "1.3.0"
kind = "source"
description = "A massively spiffy yet delicately unobtrusive compression library"
license = "Zlib"

[build]
script = "build.sh"

[publish]
registry = "sea-packages"
include = ["include/**", "lib/**"]
